<!DOCTYPE html>
<html>
<head>
  <title>Files</title>
</head>
<body>
  <ul>
    <% @filenames.each do |f| %>
    <li>
      <a href=show/<%=f %> > <%= f %> </a>
    </li>
    <% end %>
  </ul>

  <% if @reverse %>
    <a href="/" >Display Ascending </a>
  <% else %>
    <a href="/?sort=reverse" > Display Descending</a>
  <% end %>
</body>
</html>
